Repository
  .bash - Configuration files for bash

Installation
  mkdir ~/.bash
  cd ~ && for i in .bash* .profile .inputrc; do 
    mv -vi "${i}" .bash/"${i##.}"
    ln -sv .bash/"${i##.}" "${i}"
  done


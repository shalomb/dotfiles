extends: default

rules:
  line-length:
    max: 120  # Allow longer lines for readable Terraform content
  document-start: disable  # Don't require document start marker
  indentation: disable  # Ignore indentation issues for consolidated YAML
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable  # Allow flexible comment indentation

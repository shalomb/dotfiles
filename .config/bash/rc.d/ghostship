#!/bin/bash

# Ghostship prompt manager integration
# This file integrates ghostship into bash using the official init command

# Only initialize if ghostship is available
if command -v ghostship >/dev/null 2>&1; then
    # Use ghostship's official init command
    source <(ghostship init bash)
else
    echo "Warning: ghostship not found in PATH"
fi

#!/usr/bin/perl

# map

use strict;
use warnings;

use Cwd;
use File::Basename  qw[basename dirname];
use File::Find;
  no warnings 'File::Find';
use File::Glob      qw[:glob];
use File::Path      qw[make_path];
use File::Spec      qw[catfile];
  sub fsc { File::Spec->catfile(@_) }

local $\ = "\n";
local $|; $|++;

sub make_directory;
sub make_link;
sub readlink_f;

my $action = $ARGV[0] // 'add';

chdir dirname "$0"
  or die "Error changing directory to dirname($0): $!";

my @globs;
while (<DATA>) {
  chomp;
  push @globs, $_;
}
close DATA;

for my $glob ( @globs ) {

  for my $file ( my @files = bsd_glob $glob ) {
      
    my ($relative_file) = ($file =~ m[^\s*$ENV{HOME}\/(.*)$]);
    my $realfile = readlink $file // $file;

    next unless length $relative_file 
                and -e $realfile;

    printf "Committing '%s' as '%s'\n", $file, $relative_file
      if not -e $relative_file;

    if ( -d $realfile ) {
      make_directory ( $relative_file ) 
        if not -d $relative_file;

      find { 
          wanted => sub {
            my ($relative_name) = 
              ($File::Find::name =~ m[^\s*$ENV{HOME}\/(.*)$]);

            if ( -l (my $realfile = $File::Find::name) or -f _ ) {
              $realfile = readlink_f $File::Find::name if -l _;
              make_link( $realfile, $relative_name );
            }
            elsif ( -d _ ) {
              make_directory( $relative_name );
            }
            else {
              warn "Not processing '$File::Find::name', possible special file.\n";
            }
          }, 
          no_chdir => 1
        }, 
        $realfile;
    }

    elsif ( $relative_file =~ m[\/] ) { 
      make_directory  ( dirname($relative_file) ) 
        if not -d dirname($relative_file);

      make_link ( $realfile, $relative_file );
    }

    else {
      make_link ( $realfile, $relative_file );
    }
  }

}

sub make_directory {
  my $directory = shift;

  warn "Error creating directory '$directory': $!\n"
    if (  not -d $directory           and
          print "mkdir -p $directory" and
          not make_path($directory)   and
          return 1
        ); 
}

sub make_link {
  my ($target, $link) = (@_);

  warn "Error creating link '$link' to '$target': $!\n"
    if (  not -f $link                 and 
          print "ln '$target' '$link'" and
          not link $target, $link      and
          return 1
        );

}

# Courtesy: R.Schwartz
# "Expanding symbolic links (aug 99)"
#  http://www.stonehenge.com/merlyn/UnixReview/col27.html
sub readlink_f {
  my $file = shift;

  my @right = split /\//, $file;

  my @left = do {
    @right && ($right[0] eq "") ?
    shift @right :            # quick way
    split /\//, getcwd;
  };    # first element always null

  while (@right) {
    my $item = shift @right;
    next if $item eq "." or $item eq "";

    if ($item eq "..") {
      pop @left if @left > 1;
      next;
    }

    my $link = readlink fsc(@left, $item);

    if (defined $link) {
      my @parts = split /\//, $link;
      if (@parts && ($parts[0] eq "")) { # absolute
        @left = shift @parts;   # quick way
      }
      unshift @right, @parts;
      next;
    } 
    else {
      push @left, $item;
      next;
    }
  }

  return fsc(@left);
}

# TODO
#   * Add a reverse action of 'add' - consider better verbage
#   * Targets need to take on name as listed in __DATA__ not the real filename
#   * __DATA__ should really exist in a synclist file


__DATA__
~/.bin/googlize
~/.bin/osd_msg
~/.bin/qterm
~/.bin/winctrl
~/.bin/winmark
~/.config/wallpapers/wallpaper.list
~/.config/zim/accelmap
~/.config/zim/default.conf
~/.config/zim/mklinks
~/.config/zim/state.conf
~/.dircolors
~/.fehrc
~/.fluxbox/apps
~/.fluxbox/init
~/.fluxbox/keys
~/.fluxbox/menu
~/.fluxbox/overlay
~/.fluxbox/slitlist
~/.fluxbox/startup
~/.fluxbox/windowmenu
~/.gitignore
~/.gmrunrc
~/.gtkrc-2.0
~/.ideskrc
~/.idesktop
~/.inputrc
~/.local/share/applications
~/.pentadactyl/plugins/
~/.pentadactylrc
~/.profile
~/.screenrc
~/.tmux.conf
~/.vimperator/plugin/
~/.vimperatorrc
~/.Xdefaults
~/.xinitrc
~/.Xmodmap
~/.xsession
/etc/apt/sources.list.d/
/etc/apt/sources.list.d/X45_dropbox-maverick.list
/etc/apt/sources.list.d/X20_swiftfox.list_
/etc/apt/sources.list.d/O05_opera-beta-sid.list_
/etc/apt/sources.list.d/U00_debian-unofficial.org-sid.list_
/etc/apt/sources.list.d/recv-apt-key_
/etc/apt/sources.list.d/M00_debian-multimedia-sid.list_
/etc/apt/sources.list.d/X60_sidux.list_
/etc/apt/sources.list.d/G10_google-stable.list_
/etc/apt/sources.list.d/S00_skype-stable.list
/etc/apt/sources.list.d/X30_freenx-karmic.list_
/etc/apt/sources.list.d/O45_opera-beta-etch.list_
/etc/apt/sources.list.d/X70_spotify.list
/etc/apt/sources.list.d/O00_opera-sid.list_
/etc/apt/sources.list.d/600_debian-squeeze.list
/etc/apt/sources.list.d/C00_CPAN-unstable.list
/etc/apt/sources.list.d/M50_debian-multimedia-lenny.list_
/etc/apt/sources.list.d/U40_debian-unofficial.org-etch.list_
/etc/apt/sources.list.d/V40_virtualbox-etch.list_
/etc/apt/sources.list.d/500_debian-lenny.list_
/etc/apt/sources.list.d/V60_virtualbox-squeeze.list_
/etc/apt/sources.list.d/O60_opera-squeeze.list_
/etc/apt/sources.list.d/X10_mysql-jaunty.list_
/etc/apt/sources.list.d/U60_debian-unofficial.org-squeeze.list_
/etc/apt/sources.list.d/G20_google-testing.list
/etc/apt/sources.list.d/W60_wine-lamaresh-squeeze.list
/etc/apt/sources.list.d/O65_opera-beta-squeeze.list
/etc/apt/sources.list.d/O40_opera-etch.list_
/etc/apt/sources.list.d/020_debian-sid.list_
/etc/apt/sources.list.d/U50_debian-unofficial.org-lenny.list_
/etc/apt/sources.list.d/M90_debian.net-squeeze-backports.list
/etc/apt/sources.list.d/W50_wine-lamaresh-lenny.list_
/etc/apt/sources.list.d/400_debian-etch.list_
/etc/apt/sources.list.d/B60_backports.org-squeeze.list_
/etc/apt/sources.list.d/000_local.list_
/etc/apt/sources.list.d/B50_backports.org-lenny.list_
/etc/apt/sources.list.d/W00_wine-lamaresh-sid.list_
/etc/apt/sources.list.d/M40_debian-multimedia-etch.list_
/etc/apt/sources.list.d/M60_debian-multimedia-squeeze.list
/etc/apt/sources.list.d/V50_virtualbox-lenny.list
/etc/apt/sources.list.d/O50_opera-lenny.list_
/etc/apt/sources.list.d/X50_opengroupware.list_
/etc/apt/sources.list.d/X40_dropbox-karmic.list_
/etc/apt/sources.list.d/.directory
/etc/apt/sources.list.d/O55_opera-beta-lenny.list_
/etc/apt/sources.list.d/040_debian-experimental.list_
/etc/apt/sources.list.d/B40_backports.org-etch.list_
